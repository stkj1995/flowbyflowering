---
import Button from '../components/Button.astro';
import { Image } from 'astro:assets';
import Nyhedsbrev1 from '../assets/images/nyhedsbrev1.png'
import Nyhedsbrev2 from '../assets/images/nyhedsbrev2.png'
---
<script src="https://cdn.tailwindcss.com"></script>

<section class="bg-rosa-500 md:grid md:grid-cols-3 md:gap-0 pt-10 pb-20 px-5 md:px-20 md:pt-20 md:pb-20">
    <!-- Tekst og formular til højre -->
    <div class="flex flex-col justify-center bg-rosa-500 p-10 max-w-3xl mx-auto">
      <!-- Overskrift -->
      <h2 class="text-blue-500 text-50 font-WindstoneSerif mb-sm">
        Vil du blomstre med os?
      </h2>
  
      <!-- Brødtekst -->
      <p class="text-darkgreyblue-500 font-LivvicRegular text-20 mb-sm">
        Få det bedste fra Flowering direkte i din indbakke. Modtag inspiration, tips til blomsterpleje, eksklusive tilbud og nyheder om vores bæredygtige rejse.
      </p>
  
      <!-- Formular -->
      <form class="flex flex-col gap-sm">
        <input
          type="text"
          id="name"
          name="name"
          placeholder="Navn"
          class="rounded-20 border-1 border-greyblue-500 p-2 focus:ring-1 focus:ring-greyblue-600 focus:outline-none"
        />
  
        <input
          type="email"
          id="email"
          name="email"
          placeholder="Email"
          class="rounded-20 border-1 border-greyblue-500 p-2 focus:ring-1 focus:ring-greyblue-600 focus:outline-none"
        />
  
        <!-- Midlertidig Knap -->
        <button
          type="submit"
          class="bg-greyblue-500 text-white text-20 font-LivvicBold py-2 rounded-20 hover:bg-denimblue-500 transition shadow-sm"
        >
          Tilmeld dig vores nyhedsbrev
        </button>
      </form>
    </div>   

    <!-- Billeder til venstre -->
    <div class="w-full flex flex-row md:flex-col gap-2 justify-between">
        <div class="w-3/3">
        <Image src={Nyhedsbrev1} alt='billede af en holdt buket' class="rounded-40 md:mt-14 ml-0 w-48 h-60 md:w-96 md:h-full object-cover md:object-cover">
    </div></div>
    <div class="w-full flex flex-row md:flex-col gap-2">
        <div class="w-2/2">
        <Image src={Nyhedsbrev2} alt='billede af pige med to buketter' class="md:mt-56 rounded-40 w-32 h-44 md:w-96 md:h-96 object-cover md:object-cover">
    </div></div>
</section>

  